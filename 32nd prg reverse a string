#include <stdio.h>
#include <string.h>
int main() {
    char str[100];
    int len, i;
    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin);
    len = strlen(str) - 1;
    printf("Reversed string: ");
    for (i = len; i >= 0; i--) putchar(str[i]);
    printf("\n");
    return 0;
}
